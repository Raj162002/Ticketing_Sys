spring.application.name=raj
# Database URL, username, and password
spring.datasource.url=jdbc:postgresql://localhost:5432/TicketStream
spring.datasource.username=manager
spring.datasource.password=1234

# Specify the PostgreSQL driver
spring.datasource.driver-class-name=org.postgresql.Driver

# Optional: JPA settings for Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

## Disable SQL statements logging
#spring.jpa.properties.hibernate.show_sql=false
#
## Disable Hibernate formatted SQL logging (pretty-print SQL)
#spring.jpa.properties.hibernate.format_sql=false
#
## Disable Hibernate logging (including SQL statements)
#logging.level.org.hibernate.SQL=OFF
#logging.level.org.hibernate.type.descriptor.sql=OFF